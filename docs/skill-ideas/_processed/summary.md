Idea is to force the agent to summarize without word vomitting.

- Tell me what a careful reader would notice that a skim would miss
And followed up with:
- Now organize those into the three or four points I'd actually lead with if I had to present this

---
**/forge verdict (2026-08-29): ADD** as `distill` (utility, explicit-invocation only).
Distinct from `tldr` on both Overlap-check limbs — different input, different output,
and opposite method (interpretation vs verbatim extraction). Weakest `add` of the run;
`/prune` should drop it if uninvoked within a cycle. See `_assessments/2026-08-29.md`
(run 2).
