Checks whether your prompt would benefit from a rewrite before the model acts on it, shows you the rewrite, and waits for approval. Stays quiet when the prompt is already fine. Use case: Catches the vague ask that would otherwise burn a full turn producing the wrong thing. Daily. 

---
**/forge verdict (2026-08-29): DROP** (skill-fit + scope). Recurring bare-nudge case already folded into `implement`/`plan-doc`; broader want is a `CLAUDE.md` line, not a skill. See `_assessments/2026-08-29.md`.
