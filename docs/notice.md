# Notice — scratch log for skill friction

One line per note: `- <date>: <what happened> → <what it might mean for a skill>`.
Skim it whenever you are touching skills anyway; delete each line once it is actioned
or judged dead.

<!-- 2026-08-01: cleared six actioned lines — the devkit-coupling of implement /
     land-feature / plan-doc (logged twice: vacbat, then erp), venv resolution, the
     `git add -A` sweep, return-shape assertions, and inherited-vs-regression test
     failures. All fixed in those three skills, which are now symlinked into
     ~/.claude/skills so they load outside devkit. -->

- 2026-08-02: diagnosing an HVAC unit "offline", I concluded the DHCP reservation was missing from the absence of its effect, without reading the erp dnsmasq config that governed it — user corrected me, the binding was there all along → diagnose could say: before blaming config for being absent, go read that config; absence of effect is not absence of cause.
- 2026-08-02: landing deebot from the devkit session, `git add -A` staged devkit — an earlier `cd` into the target repo had silently reverted to the primary working dir between Bash calls → land-feature (and any skill running git on a repo that is not the primary working dir) could say: pass `git -C <repo>` explicitly rather than relying on a persisted `cd`, and check the staged-path list names the repo you meant before committing.
- 2026-08-02: an ARP-vs-bindings comparison returned "absent" for all 16 rows because `ip neigh show dev X` drops a field my awk indexed on; I read the uniform result as a parsing bug, not a finding → diagnose could say: a derived table that comes back uniform (all-absent, all-zero, all-null) is a parser smoke test failing, not evidence — sanity-check the intermediate before interpreting it.
- 2026-08-02: auditing 146 sessions I reported "planit never fires, agents unused" — but every skill and agent dir was created 07-31/08-02, so a 30-day session history was being compared against 2-day-old tooling; user caught it → any skill that analyses history could say: check the mtime/creation date of the thing you are measuring before reading a zero as a signal — a zero over a window where the subject did not exist is not evidence.
- 2026-08-02: session audit found 49 user interruptions and 11 compactions across 146 sessions, concentrated where a bare nudge ("continue", "implement") opened a multi-file task → planit/implement could say: when the opening prompt is a bare nudge and no plan doc is in play, restate the intended scope in one line and start, rather than running long on an inferred scope.
- 2026-08-05: erp UI redesign session — `run` skill's browser-driven pattern assumes `chromium-cli`, which wasn't installed; had to fall back to `pip install playwright && playwright install chromium` (no `--with-deps`, since `apt` needed sudo password) and hand-write a driver script → run/playwright.md could note this fallback path explicitly (pip install + browser-only download, skip `--with-deps` when sudo is unavailable) so it's not rediscovered each time.
- 2026-08-08: docflow dark-theme diagnosis found real stale build artifacts (theme.css generated from the wrong palette) and reported that as the fix; after rebuilding, user said "still same" — actual cause was `localStorage.theme==='light'` pinning the browser to the light palette, which the rebuild couldn't touch since the two palette's light blocks were byte-identical → diagnose could say: for a UI-rendering bug with client-side persisted state (localStorage/cookies/cached DOM class), check that state before/alongside the build pipeline — a correct-looking fix that doesn't visibly change anything is a cue to look at what's actually selecting the rendered branch, not just re-verify the artifact.
- 2026-08-08: implementing docflow Plan 015 (regression suite), three of the plan's assumed behaviors were wrong once checked against the real code — abandon_session doesn't delete pages, corners.js is only fully wired on adjust.html not review.html, and grid's adjust route allows cross-workspace reads — plan-doc could say: for a plan step that names route/behavior to assert, read that source before locking the assertion into the plan text, since the implementer otherwise has to silently override the plan mid-task.
- 2026-08-08: a docflow UI test comparing body background color before/after a theme toggle read the DOM mid-transition (body has `transition-colors duration-200`) and got a false "identical" failure until an explicit wait was added → a browser-testing skill/guidance could note: after toggling a CSS class that drives a `transition-*` property, wait past the duration before reading computed style, not just past the JS event.
- 2026-08-09: landing docflow Plan 016 on a branch whose working tree already held two other uncommitted streams (an unlanded Plan 015 index, plus unrelated palette/icon polish) sitting in the same files I edited — no interactive `git add -p` tool is available in this environment, so a clean per-plan hunk split isn't achievable, and I only discovered the tangle at commit time after already doing all the edits → land-feature could say: check `git status --short` for a dirty/mixed tree *before* starting edits, not just before committing, since a clean split may be impossible without interactive staging and the commit strategy is the user's call to make up front.
- 2026-08-09: mid-implement (docflow Plan 017) a tool-use rejection/interruption was followed by a plain `.venv/bin/python -m pytest` call failing with "No such file or directory" — the interruption had silently reset the Bash cwd away from the repo root, same failure mode as the earlier `cd`-reverts-between-calls note but triggered by an interruption rather than an explicit `cd` → any skill issuing repo-relative shell commands could say: after any tool-use interruption/rejection, re-verify cwd (or just always use `cd /abs/path && cmd` / absolute paths) rather than assuming it survived.
- 2026-08-09: implementing docflow Plan 019, a new test using generic names ("Owner WS"/"Empty WS") passed alone but failed inside the full suite — the test DB is session-scoped (conftest.py), so rows from earlier tests with the same names/text leaked into the assertion → implement/plan-doc could say: in a repo with a session-scoped test DB, give new test fixtures unique/plan-tagged names (not "WS", "Doc") so an isolated pass doesn't mask a full-suite collision.
- 2026-08-11: designit round 1 for an infra/server migration plan (fujitsu Ansible bootstrap) proposed hardening changes with no rollback path; only surfaced after user asked "what's the failure plan" — revealed zero restore points existed (no snapshots, dead backups, single fragile network link) → designit could say: for infra/ops/server-touching plans, round 1 should include a rollback/safety-net question by default (snapshots, backup verification, revert path for network/firewall changes), not wait for the user to ask.
- 2026-08-14: implementing plan 002 (dnsmasq), four separate command-block mistakes handed to the user across one session — wrong iface name (enp3s0), told to check `journalctl -u dnsmasq` when the unit logs to a file not the journal, `sudo wc -l < file` (redirect runs unprivileged, not under sudo), and a snippet mixing control-node `ansible-playbook` calls with target-host `grep`/`systemctl` in one block — every code/config change that session was validated against the live box before handoff, but instructions *to the user* got no equivalent check → implement (and any skill that hands the user a command to run rather than running it itself) could say: a multi-line shell snippet handed to the user is a deliverable like a diff — dry-run or reason through each line (which host, which privilege, does the redirect apply where you think) before sending, not just after the user hits the error.
- 2026-08-16: implementing server-scripts plan 006 step 4 as authored (delete the dnsmasq sudoers grant + its var), the literal deletion would have broken live DNS/DHCP reload immediately — the grant is still needed by the host erp process until step 6 (not yet landed) swaps it for a container; caught only by flagging it and asking, not by anything in the implement flow → implement could say: before executing a plan step's literal delete/revoke instruction, check whether anything *currently live* still depends on it pending a later, not-yet-landed step — if so, gate the removal behind a flag the later step flips, rather than deleting on the step's own schedule.
- 2026-08-29: asked to review makeitklick.com.au copy — WebFetch got 403 (Cloudflare bot block), and the page HTML is a client-rendered skeleton that pulls all real copy from a separate `site.js` `SITE={}` object, so even a successful fetch of `/` would have shown no headline/bio text → any "look at my site / review this page" task could note: when WebFetch 403s, retry with curl + a browser User-Agent; if the HTML has near-empty content divs and a `<script src=...>`, fetch that JS too — the copy often lives in a JS config object, not the HTML.
- 2026-08-28: implementing wow plan 006 (wa_lens port) end-to-end, the land-feature closeout again hit a large in-flight restructure — the repo tree carried ~40 unrelated untracked/modified paths ("raids and dungeons round1"), and three files my plan-12 step edited (CLAUDE.md, docs/wcl-analysis-methods.md, .claude/skills/wow-log-research/SKILL.md) were themselves uncommitted pending work, so no clean per-plan commit was possible. Same failure as the 2026-08-09 note, and the dirty tree was visible in the session-start `git status` the whole time → implement could say: when step 1 (find the plan) sees a dirty/mixed tree, surface the commit-strategy question to the user *then*, before working the steps — don't carry it to land-feature's step 7.
