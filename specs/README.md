# Plans (specs/)

Numbered plans under the spec-driven pipeline (`.specify/memory/constitution.md`).
Each plan is `specs/NNN-<slug>/plan.md` (+ `spec.md`, `review.md`, `gates/` as the
pipeline produces them). Numbers are permanent — never reused, never renumbered.
Status is tracked in the table below only. Vocabulary: `authored` → `implemented` →
`superseded` → `abandoned` (see the constitution).

Plan 001 predates this directory and lives at `docs/plans/001-skill-pipeline-framework.md`.
002 onward live here.

| # | Title | Status | Notes |
|---|-------|--------|-------|
| 002 | forge — assess skill ideas | implemented | Adversarial idea→skill assessor; utility skill |
