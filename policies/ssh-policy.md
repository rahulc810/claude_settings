# SSH policy

  Use ssh to diagnose and gather server(also called fujitsu) details

  ## Targets

  - **server**: `DNS=fujitsu` , `ip: 192.168.1.115` `user: agent`
  - **command** `ssh -i /home/rahul/.ssh/agent_audit_key agent@fujitsu`